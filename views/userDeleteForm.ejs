<!DOCTYPE html>
<html>
  <% if (currentUser) { %>
    <head>
      <title>Delete user</title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
      <%- include('navbar') %>
      <h1>Delete user</h1>
      <%= currentUser.username %>
      <form id="deleteUser" action="/uploads/user/<%= currentUser.id %>/delete" method="post">
          <button type="submit" class="deleteUserBtn">Delete User</button>
      </form>
      <a href="/uploads/user/<%= currentUser %>/home" class="cancel">Cancel</a>
    </body>
  <% } else { %>
    <%- include('pageNotFound') %>
  <% } %>
</html>