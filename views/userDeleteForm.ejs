<!DOCTYPE html>
<html>
  <% if (currentUser) { %>
    <head>
      <title>Delete user</title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
      <h1>Delete user</h1>
      <%= currentUser.username %>
      <form id="deleteUser" action="/uploads/user/<%= currentUser.id %>/delete" method="post">
          <button type="submit" class="deleteUserBtn">Delete User</button>
      </form>
      <a href="/uploads/user/<%= currentUser %>/home" class="cancel">Cancel</a>
    </body>
  <% } else { %>
    <head>
      <title>Page not accessible</title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
      <h1>Page not accessible</h1>
      <a href="/uploads" class="welcomeLink">Back to welcome page</a>
    </body>
  <% } %>
</html>