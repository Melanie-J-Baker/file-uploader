<!DOCTYPE html>
<html>
  <% if (currentUser) { %>
    <head>
      <title><%= file.name %></title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
      <h1><%= file.name %></h1>
      <img src="<%= file.url %>" class="fileImage" alt="File preview">
      <p class="fileDetail">Size: <%= file.size_mb %> mb</p>
      <p class="fileDetail">Upload time: <%= file.upload_time %></p>
      <button type="button">Download file</button>
      <a href="/uploads/file/<%= file.id %>/update" class="updateFile">Update file</a>
      <a href="/uploads/file/<%= file.id %>/delete" class="deleteFile">Delete file</a>
      <% if (file.folder_id) { %>
        <a href="/uploads/folder/<%= file.folder_id %>" class="folderLink">Go back to folder</a>
      <% } else { %>
        <a href="/uploads/user/<%= currentUser.id %>/home" class="homeLink">Go back to home</a>
      <% } %>
    </body>
  <% } else { %>
    <head>
      <title>Page not accessible</title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
      <h1>Page not accessible</h1>
      <a href="/uploads" class="welcomeLink">Back to welcome page</a>
    </body>
  <% } %>
</html>